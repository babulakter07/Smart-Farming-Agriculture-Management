@model Firming_Solution.Domain.Entities.Cost
@{
    ViewData["Title"] = "Record Cost";
}

<div class="row justify-content-center">
    <div class="col-md-8 col-lg-7">
        <div class="caw-card">
            <div class="caw-section-title"><i class="fas fa-receipt me-2"></i>Record Cost</div>
            <form asp-action="Create" method="post">
                <div asp-validation-summary="ModelOnly" class="alert alert-danger py-2 small mb-3"></div>

                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label asp-for="FarmId" class="caw-form-label">Farm <span class="text-danger">*</span></label>
                        <select asp-for="FarmId" asp-items="ViewBag.Farms" class="form-select">
                            <option value="">-- Select Farm --</option>
                        </select>
                        <span asp-validation-for="FarmId" class="text-danger small"></span>
                    </div>
                    <div class="col-md-6 mb-3">
                        <label asp-for="BatchId" class="caw-form-label">Batch (optional)</label>
                        <select asp-for="BatchId" asp-items="ViewBag.Batches" class="form-select">
                            <option value="">-- Select Batch --</option>
                        </select>
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label asp-for="CostCategory" class="caw-form-label">Category <span class="text-danger">*</span></label>
                        <select asp-for="CostCategory" asp-items="Html.GetEnumSelectList<Firming_Solution.Domain.Enums.CostCategory>()" class="form-select"></select>
                    </div>
                    <div class="col-md-6 mb-3">
                        <label asp-for="CostDate" class="caw-form-label">Date <span class="text-danger">*</span></label>
                        <input asp-for="CostDate" class="form-control" type="date" />
                    </div>
                </div>

                <div class="mb-3">
                    <label asp-for="Amount" class="caw-form-label">Amount (৳) <span class="text-danger">*</span></label>
                    <input asp-for="Amount" class="form-control" type="number" step="0.01" min="0.01" placeholder="0.00" />
                    <span asp-validation-for="Amount" class="text-danger small"></span>
                </div>

                <div class="mb-4">
                    <label asp-for="Description" class="caw-form-label">Description</label>
                    <textarea asp-for="Description" class="form-control" rows="3" placeholder="Details about this cost..."></textarea>
                </div>

                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-caw-primary flex-fill">
                        <i class="fas fa-save me-2"></i>Record Cost
                    </button>
                    <a asp-action="Index" class="btn btn-outline-secondary flex-fill">
                        <i class="fas fa-times me-2"></i>Cancel
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
